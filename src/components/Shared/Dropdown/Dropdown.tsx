"use client";

import {
  Dropdown as DropdownADS,
  DropdownProps,
} from "@ama-pt/agora-design-system";

export default function Dropdown(args: DropdownProps) {
  return <DropdownADS {...args} />;
}
