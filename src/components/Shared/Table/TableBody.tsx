"use client";

import {
  TableBody as TableBodyADS,
  TableBodyProps,
} from "@ama-pt/agora-design-system";

export default function TableBody(args: TableBodyProps) {
  return <TableBodyADS {...args} />;
}
