"use client";

import {
  TableRow as TableRowADS,
  TableRowProps,
} from "@ama-pt/agora-design-system";

export default function TableRow(args: TableRowProps) {
  return <TableRowADS {...args} />;
}
