"use client";

import {
  TableFooter as TableFooterADS,
  TableFooterProps,
} from "@ama-pt/agora-design-system";

export default function TableFooter(args: TableFooterProps) {
  return <TableFooterADS {...args} />;
}
