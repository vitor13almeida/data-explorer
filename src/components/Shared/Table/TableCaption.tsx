"use client";

import {
  TableCaption as TableCaptionADS,
  TableCaptionProps,
} from "@ama-pt/agora-design-system";

export default function TableCaption(args: TableCaptionProps) {
  return <TableCaptionADS {...args} />;
}
