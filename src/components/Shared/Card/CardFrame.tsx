"use client";

import {
  CardFrame as CardFrameADS,
  CardFrameProps,
} from "@ama-pt/agora-design-system";

export default function CardFrame(args: CardFrameProps) {
  return <CardFrameADS {...args} />;
}
