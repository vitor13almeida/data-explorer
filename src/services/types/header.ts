export interface LanguageOption {
  value: string;
  label: string;
  abbr: string;
}

export interface EcosystemItem {
  label: string;
  href: string;
  icon: string | null;
  bgColor: string | null;
}
